# Supabase (optional). If set, auth and data sync to Supabase so data is shared across devices.
# Get these from your Supabase project: Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
