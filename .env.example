# Supabase (optional). If set, auth and data sync to Supabase so data is shared across devices.
# Get these from your Supabase project: Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# OpenAI (optional). Powers the LLM-backed voice assistant for smarter command parsing.
# Get a key from https://platform.openai.com/api-keys
# Set this in Vercel → Project Settings → Environment Variables (do NOT expose in client code).
OPENAI_API_KEY=
